# CLI Task Manager Pro

A lightweight and interactive Python CLI script designed for Termux and Linux environments.

## Features
- Interactive ANSI colored interface.
- Automatic data persistence using JSON (`tasks_data.json`).
- Dynamic progress bar simulation.

## Usage
1. Clone or download `tasks_app.py`.
2. Run the application:
```bash
python tasks_app.py

